import { Card, Typography } from "@unisane/ui";

export const metadata = {
  title: "Card · Unisane UI",
  description: "Elevated and outlined surfaces.",
};

# Card

Cards group related information inside layered surfaces. Use outlined cards for data summaries and elevated cards for priority content.

<ComponentPreview
  title="Card variants"
  description="Outlined and elevated examples."
>
  <Card variant="outlined" padding="md" className="min-w-[220px]">
    <CardHeader>
      <Typography variant="labelSmall">Node Brief</Typography>
      <CardTitle>Telemetry Sync</CardTitle>
    </CardHeader>
    <CardContent>
      Latency stable. Next diagnostic sweep scheduled in 2 hours.
    </CardContent>
  </Card>
  <Card variant="elevated" padding="md" className="min-w-[220px]">
    <CardHeader>
      <Typography variant="labelSmall">Escalation</Typography>
      <CardTitle>Priority Queue</CardTitle>
    </CardHeader>
    <CardContent>
      7 critical items need approval before deployment.
    </CardContent>
  </Card>
</ComponentPreview>

```tsx
import { Card } from "@unisane/ui";

<Card variant="outlined" padding="md">
  <Card.Header>
    <Card.Title>Telemetry Sync</Card.Title>
  </Card.Header>
  <Card.Content>Latency stable.</Card.Content>
</Card>
```
