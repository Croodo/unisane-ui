import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@unisane/ui";

export const metadata = {
  title: "Table · Unisane UI",
  description: "Structured data layouts.",
};

# Table

Tables provide dense layouts for tabular data with hover and header treatments.

<ComponentPreview
  title="Inventory table"
  description="Structured rows with column headers."
>
  <div className="w-full">
    <Table>
      <TableHeader>
        <TableRow>
          <TableHead>Asset</TableHead>
          <TableHead>Status</TableHead>
          <TableHead>Load</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        {[
          { asset: "Core Alpha", status: "Active", load: "42%" },
          { asset: "Node Beta", status: "Active", load: "58%" },
          { asset: "Array Gamma", status: "Maintenance", load: "0%" },
        ].map((row) => (
          <TableRow key={row.asset}>
            <TableCell>{row.asset}</TableCell>
            <TableCell>{row.status}</TableCell>
            <TableCell>{row.load}</TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
  </div>
</ComponentPreview>

```tsx
import { Table, TableHeader, TableRow, TableHead } from "@unisane/ui";

<Table>
  <TableHeader>
    <TableRow>
      <TableHead>Asset</TableHead>
    </TableRow>
  </TableHeader>
</Table>
```
