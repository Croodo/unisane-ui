import { Button, Icon } from "@unisane/ui";

export const metadata = {
  title: "Button · Unisane UI",
  description: "Primary actions and tonal variants.",
};

# Button

Buttons communicate primary actions and state. Use filled buttons for primary actions, tonal for secondary emphasis, and outlined or text for tertiary actions.

<ComponentPreview
  title="Variants"
  description="Filled, tonal, outlined, and text treatments."
>
  <Button variant="filled">Execute</Button>
  <Button variant="tonal">Queue</Button>
  <Button variant="outlined">Plan</Button>
  <Button variant="text">Review</Button>
</ComponentPreview>

<ComponentPreview
  title="With icons"
  description="Attach leading and trailing icons for visual cues."
>
  <Button variant="filled" icon={<Icon symbol="bolt" />}>Deploy</Button>
  <Button
    variant="outlined"
    trailingIcon={<Icon symbol="arrow_forward" />}
  >
    Continue
  </Button>
</ComponentPreview>

```tsx
import { Button } from "@unisane/ui";

<Button variant="filled">Execute</Button>
<Button variant="outlined">Review</Button>
```
