import { Switch } from "@unisane/ui";

export const metadata = {
  title: "Switch · Unisane UI",
  description: "Binary toggles with optional icons.",
};

# Switch

Switches enable or disable system-level behavior with immediate feedback.

<ComponentPreview
  title="Toggles"
  description="Standard switch with optional icon affordance."
>
  <div className="flex flex-col gap-3u">
    <Switch label="Enable compact density" defaultChecked icons />
    <Switch label="Reduce motion" />
    <Switch label="Disable haptics" disabled />
  </div>
</ComponentPreview>

```tsx
import { Switch } from "@unisane/ui";

<Switch label="Reduce motion" />
```
